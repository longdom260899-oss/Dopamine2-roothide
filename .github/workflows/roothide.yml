name: Build Stealth JB

on:
  workflow_dispatch:
  push:

jobs:
  build:
    runs-on: macos-14
    steps:
      - name: Checkout
        run: |
          set -x
          git clone --recursive https://github.com/roothide/Dopamine2-roothide ${{ github.workspace }}

      - name: Install Procursus
        uses: dhinakg/procursus-action@main
        with:
          packages: ldid findutils sed coreutils make

      - name: Install THEOS
        run: |
          set -x
          export BASEDIR="$(pwd)"
          export THEOS="${BASEDIR}/theos"
          mkdir -p $THEOS
          curl -fsSL https://raw.githubusercontent.com/roothide/theos/master/bin/install-theos >> install-theos.sh
          gsed -E "/^\s*get_theos\s*$/,+1 s/^(\s*)(get_sdks)\s*$/\1mkdir -p \${THEOS}\/sdks\n\1touch \${THEOS}\/sdks\/sdk\n\1\2/g" -i install-theos.sh
          bash install-theos.sh
          curl -L https://github.com/theos/sdks/releases/latest/download/iPhoneOS16.5.sdk.tar.xz --output $THEOS/sdks/iPhoneOS16.5.sdk.tar.xz
          gunzip $THEOS/sdks/iPhoneOS16.5.sdk.tar.xz
          tar -xf $THEOS/sdks/iPhoneOS16.5.sdk.tar -C $THEOS/sdks
          rm $THEOS/sdks/iPhoneOS16.5.sdk.tar
          cd $BASEDIR

      - name: Build and Install trustcache
        run: |
          set -x
          git clone https://github.com/CRKatri/trustcache
          cd trustcache
          export CFLAGS="$CFLAGS -I$(brew --prefix openssl)/include -arch arm64"
          export LDFLAGS="$LDFLAGS -L$(brew --prefix openssl)/lib -arch arm64"
          gmake -j$(sysctl -n hw.physicalcpu) OPENSSL=1
          sudo cp trustcache /opt/procursus/bin/

      - name: Install libarchive
        run: |
          brew install libarchive

      - name: Build Dopamine (Includes BaseBin)
        run: |
          set -x
          export BASEDIR="$(pwd)"
          export THEOS="${BASEDIR}/theos"
          rm -rf $(xcrun --sdk iphoneos --show-sdk-path)/usr/include/xpc $(xcrun --sdk iphoneos --show-sdk-path)/usr/include/xpc.modulemap
          gmake -j$(sysctl -n hw.physicalcpu)

      - name: Advanced Stealth Modification
        run: |
          cd Application
          unzip -q Dopamine.tipa
          cd Payload/*.app
          
          echo "=== PHASE 1: Identity Masking ==="
          plutil -replace CFBundleIdentifier -string "com.apple.kernel.health-check" Info.plist
          plutil -replace CFBundleDisplayName -string "System Health" Info.plist
          plutil -replace CFBundleName -string "System Health" Info.plist
          
          echo "=== PHASE 2: Remove Visual Traces ==="
          rm -f AppIcon*.png
          
          echo "=== PHASE 3: Remove URL Schemes ==="
          plutil -remove CFBundleURLTypes Info.plist 2>/dev/null || echo "No URL schemes"
          
          echo "=== PHASE 4: Fake System Version ==="
          plutil -replace CFBundleVersion -string "1.0" Info.plist
          plutil -replace CFBundleShortVersionString -string "1.0" Info.plist
          
          echo "=== PHASE 5: Remove Jailbreak Metadata ==="
          plutil -remove UIBackgroundModes Info.plist 2>/dev/null || true
          plutil -remove LSApplicationQueriesSchemes Info.plist 2>/dev/null || true
          
          echo "=== PHASE 6: Fake Minimum OS ==="
          plutil -replace MinimumOSVersion -string "15.0" Info.plist
          
          echo "=== PHASE 7: Package ==="
          cd ../..
          zip -qr SystemHealth.tipa Payload
          
          echo "=== SUCCESS ==="
          ls -lh SystemHealth.tipa

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SystemHealth-Stealth
          path: Application/SystemHealth.tipa
          retention-days: 30
